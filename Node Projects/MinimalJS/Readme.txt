
npm install: install stuff in package.json

npm run: runs the "start" script

Do npm run, then open up browser to http://localhost:8081/src/index.html
Make sure no error in url, since on error, http-server will just be blank (not even display an error message)