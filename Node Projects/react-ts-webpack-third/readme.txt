Prod builds will minify / uglify
Dev builds will use inline source map

Both will bundle which also includes react & react-dom

Cannot get live reload with webpack-dev-server to work, could be complication with TypeScript