Trying to create a minimal TypeScript project that uses React (TSX) and have a single JS file rolled up.

Failed at the moment, there are warnings from rollup because it cannot resolve react & react-dom. As long 
as you include the react / react-dom JS files in the HTML, then things will work.